定义：将一个类的接口转换成客户希望的另一个接口。Adapter模式使得原本由于接口不兼容而不能一起工作的那些类可以一起工作。
使用场景：系统的行为和数据都正确，但接口不符，我们应该考虑使用适配器，目的是使控制范围之外的一个原有对象与某个接口匹配。
         适配器模式主要应用于希望复用的一些现存的类，但是接口与复用环境要求不一致的情况。使用适配器模式后，客户端代码可以统一的调用
         同一接口，变得更简单更直接，更紧凑。
角色：Client，Target，Adapter，Adaptee。